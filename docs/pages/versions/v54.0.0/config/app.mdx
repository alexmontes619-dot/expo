import React from "react";
import { View, Text, StyleSheet, TouchableOpacity } from "react-native";

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Landscape / Irrigation Checklist</Text>

      <Text style={styles.subtitle}>Sistema de trabajo diario</Text>

      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Crear nueva tarea</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.buttonSecondary}>
        <Text style={styles.buttonSecondaryText}>Ver checklist</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 24,
    justifyContent: "center",
    backgroundColor: "#f4f7f2",
  },
  title: {
    fontSize: 28,
    fontWeight: "bold",
    marginBottom: 10,
    color: "#1f3d2b",
    textAlign: "center",
  },
  subtitle: {
    fontSize: 16,
    marginBottom: 30,
    color: "#4b604f",
    textAlign: "center",
  },
  button: {
    backgroundColor: "#2f6b3f",
    padding: 16,
    borderRadius: 14,
    marginBottom: 14,
  },
  buttonText: {
    color: "white",
    fontSize: 17,
    fontWeight: "bold",
    textAlign: "center",
  },
  buttonSecondary: {
    borderWidth: 2,
    borderColor: "#2f6b3f",
    padding: 16,
    borderRadius: 14,
  },
  buttonSecondaryText: {
    color: "#2f6b3f",
    fontSize: 17,
    fontWeight: "bold",
    textAlign: "center",
  },
});
